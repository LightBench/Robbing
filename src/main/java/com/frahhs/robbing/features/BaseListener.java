package com.frahhs.robbing.features;

import org.bukkit.event.Listener;

/**
 * Abstract base class for listeners in the plugin.
 */
public abstract class BaseListener extends BaseFeature implements Listener {}
