package com.frahhs.robbing;

import com.frahhs.robbing.feature.Base;
import org.bukkit.event.Listener;

/**
 * Abstract base class for listeners in the plugin.
 */
public abstract class BaseListener extends Base implements Listener {}
